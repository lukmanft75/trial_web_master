RESET C & B:
===========
	TRUNCATE `all_data_update`;
	TRUNCATE `booking_facilities`;
	TRUNCATE `bpjs`;
	TRUNCATE `candidates`;
	TRUNCATE `candidate_course`;
	TRUNCATE `candidate_educations`;
	TRUNCATE `candidate_families`;
	TRUNCATE `candidate_homebases_history`;
	TRUNCATE `candidate_infos`;
	TRUNCATE `candidate_projects_history`;
	TRUNCATE `candidate_relations`;
	TRUNCATE `candidate_work_experiences`;
	TRUNCATE `contract_logs`;
	TRUNCATE `joborder`;
	TRUNCATE `joborder_allowances`;
	TRUNCATE `joborder_benefits`;
	TRUNCATE `jurnals`;
	TRUNCATE `jurnal_details`;
	TRUNCATE `log_histories`;
	TRUNCATE `transactions`;
	TRUNCATE `wcc`;
